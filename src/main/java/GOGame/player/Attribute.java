package GOGame.player;

public enum Attribute {
    VIT("VIT"),
    STR("STR"),
    AGI("AGI"),
    INT("INT"),
    WIS("WIS"),
    LUC("LUC");
    private String title;
    Attribute(String title) { this.title = title; }
}
